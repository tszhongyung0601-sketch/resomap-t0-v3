# ResoMap T0 V3 — 第二輪實作 Prompt

> 只改 V3（`resomap-work/resomap-t0-v3`）。V2 不動。
> 參考：去趣（Chicktrip）的行程規劃。只學它的結構，不抄它的視覺；ResoMap 的橘色、字級、元件照舊。
> 前提：ResoMap **目前沒有任何合作商家**。畫面上出現的餐廳、住宿都是 OpenStreetMap 上的真實店家，
> 不能寫成「合作」或「推薦」。

## 角色

你是產品專案經理兼設計總監。所有決定都以「旅客站在路邊、單手拿手機」為準：少打字，每次點擊都通往真的東西。

---

## 一、首頁地圖：分成景點、餐廳、住宿三種點

| 決策 | 內容 |
|---|---|
| 資料來源 | OpenStreetMap（Overpass），抓定位附近真實存在的餐廳和住宿 |
| 篩選 | 地圖上方一排按鈕：全部／🎧 景點／🍴 餐廳／🛏 住宿 |
| icon | 景點＝橘色耳機（原本的）；餐廳＝紅色圓底加刀叉；住宿＝靛藍色圓底加床；其他景點＝灰點（原本的） |
| 點下去 | 名稱、類型（餐廳／咖啡廳／速食／飯店／民宿…）、距離、「資料：OpenStreetMap・非 ResoMap 合作商家」 |
| 動作 | 導航、加入行程、收藏；住宿多兩顆：在 Klook 找這間、在 KKday 找這間（飯店搜尋，帶聯盟 ID） |

- **餐廳**：amenity = restaurant、cafe、fast_food，半徑 1.2 km，最多 12 家，同名只留一家。
- **住宿**：tourism = hotel、hostel、guest_house、motel，半徑 3 km，最多 8 間。
- **離線備份**：Overpass 是公共服務，常常限流（實作時就遇到了）。所以預設位置（新店）附近的真實店家
  另外存一份在 `src/data/osmSnapshot.ts`，2026-09-22 抓取。即時查詢失敗、而且人在預設位置附近時，就用這份。
  地圖角落本來就有「© OpenStreetMap contributors」標示。
- **加入行程**：新增一種行程項目 `StopRef { kind: "place" }`，把名稱、座標、類型直接存在項目裡
  （因為它不在 ResoMap 的資料庫裡）。
- **收藏**：`saved` 多一格 `places`，收藏頁也要列出來。

## 二、行程頁：學去趣的分頁

上方分頁：**總覽｜第 1 天｜第 2 天｜…｜＋｜－**

- 切換分頁要取代目前這一頁，不要疊新頁（新增 `nav.replace`），所以按返回會直接回到行程列表。
- **＋**：在最後加一個空白的新一天，日期和晚數跟著更新。
- **－**：刪掉最後一天。那天有景點的話，先跳確認框。只剩一天時停用。

### 總覽

1. 日期、旅伴（原本就有）。
2. **旅遊指南**：一排 5 個大圖示：機票／住宿／交通／租車／檔案夾。
   - 前四個點下去，都開 V3 的搜尋結果頁，並帶入**這趟的地點和日期**。
   - 檔案夾＝原本的「旅行文件」。
3. **兩張卡片並排**：
   - **共同記帳**：「N 筆花費」加一顆＋，點了進旅費頁。
   - **行李清單**：「已打包 5／24」，點了進清單頁。
4. 每日行程卡片（原本就有），點了切到那一天的分頁。
5. 這趟需要的（門票、住宿提醒，原本就有）、雲端同步。

### 行李清單（新頁面 `packing`）

- 依行程自動產生，每一項寫出原因，例如「因為第 2 天可能下雨」。
  - 基本：證件、錢包、手機、充電器、行動電源、換洗衣物（N 套）、盥洗用品、常備藥品。
  - 出國：護照、外幣或信用卡、轉接頭、eSIM 或網卡、旅平險資料。
  - 天氣預報有雨：雨具。
  - 行程裡有租車據點，或某天交通方式是開車、機車：駕照（出國要國際駕照）。
  - 溫泉（北投、礁溪…）：泳衣、毛巾。
  - 海邊（七星潭、旗津…）：防曬、太陽眼鏡。
  - 山區（太魯閣、合歡山…）：保暖外套、好走的鞋。
  - 有語音導覽的景點：耳機。
- 可以勾選、可以自己新增、可以刪除。存在 localStorage（`resomap_v3_packing`），每趟行程分開存。

### 每一天

- 日期列下面加一排：左邊是**交通方式**按鈕「🛵 機車 ▾」，右邊是 **⚡ 一鍵排序**。
- **交通方式**底部面板：自行安排、開車、大眾運輸、步行、機車，下面兩顆按鈕：
  - **全部更新**：這一天每段路都改成新的交通方式，重算路上時間，後面景點的時間跟著順延或提前（第一站的時間不動）。
  - **部分更新**：只記住這一天的預設交通方式，之後新加的景點才套用。
  - **自行安排**：路段只寫「自行安排」，不顯示時間，景點時間也不動。
  - 路上時間估算：直線距離乘以繞路係數，再除以各交通方式的平均速度，加上等車或停車時間。
- **一鍵排序**底部面板：
  - 起點：預設是目前的第一站，可以改。
  - 終點：預設「不指定」，可以指定某一站。
  - 用最近鄰居法排出最短路線，再用 2-opt 優化。路上時間用這一天的交通方式算，時間從起點往後重算。
  - 排完出現「復原」，按了回到排序前。
  - 分開走的日子（有兩條路線）先不支援，會說明原因。
- **每個景點下面加一條「優惠」細條**：
  - 這個景點有收錄的門票商品（例如台北101）：顯示「商品名・NT$ 示意價」，點了開商品頁。
  - 沒有收錄的：顯示「找『日月潭』的門票與體驗」，點了開搜尋結果頁。
  - 不寫假的商品名稱或價格。
- 時間重算只在旅客主動按「全部更新」或「一鍵排序」時才發生。手動拖曳還是照原本「只往後推、不往前拉」的規則。

## 三、搜尋結果頁：加日期、機票、交通

- 分類增加：**交通**（Klook `tab_key=30`，KKday `CATEGORY_055`）和**機票**（只有 Klook，KKday 沒有賣）。
  更多優惠首頁的分類圖示維持 4 個。
- **日期列**：從行程進來時自動帶入，可以改；從首頁搜尋時是選填。
- **住宿＋Klook＋查得到城市代碼**：開 `klook.com/zh-TW/hotels/searchresult/?check_in&check_out&city_id…`，真的帶入日期。
  查不到城市代碼時，退回一般搜尋頁。
- **機票＋Klook**：兩張卡片，**去程**（台北→目的地、出發日）和**回程**（目的地→台北、回程日），各自是單程直連。
  帶回程日期會查不到航班（實測），所以拆成兩條單程。目的地沒有機場代碼時（國內大多數城市），開 Klook 機票首頁讓旅客自己選。
- **KKday**：日期不會寫進網址，卡片上寫「日期請在 KKday 頁面選擇」。

### Klook 代碼（2026-09-22 從 Klook 飯店搜尋的自動完成 API 取得）

- **飯店城市代碼**：台北 19、新北 6488、台中 25、台南 164、高雄 22、宜蘭 42、花蓮 20、台東 47、南投 25303、
  嘉義市 436、嘉義縣 5243、屏東 7992、東京 28、大阪 29、京都 30、首爾 13。
- **別名**：日月潭→南投、九份／淡水／北海岸→新北、阿里山→嘉義縣、墾丁→屏東、東海岸→台東。
- **機票地點代碼**：台北 17343、東京 9807、大阪 14472、首爾 9835（京都用大阪）。

### 日期

- 行程的日期只存了月和日（例如「8 月 12 日」）。換算成完整日期時，取「今天之後第一次遇到的那個日期」，
  這樣帶去平台的日期一定是未來、可以訂的。

---

## 驗收

- [ ] 地圖篩選四個按鈕都有作用；餐廳、住宿點下去的面板動作都能用；Overpass 失敗時會用離線備份
- [ ] 加入行程的餐廳會出現在行程裡，可以導航；收藏頁看得到
- [ ] 分頁切換不會疊頁；＋、－ 會改變天數和日期
- [ ] 總覽的 4 個 OTA 按鈕都帶入地點和日期；Klook 住宿實際打開時日期正確
- [ ] 交通方式的全部更新會重算時間，部分更新只影響新加的景點；一鍵排序後可以復原
- [ ] 行李清單依行程產生、可以勾選和新增，重新整理後還在
- [ ] 手機寬度 375px 沒有橫向捲動；`npm run build` 通過
